[deploy]
startCommand = "echo 'Starting...' && mkdir -p ~/.nanobot && cp config.json ~/.nanobot/config.json && echo 'Config copied' && cat ~/.nanobot/config.json && echo 'Starting nanobot...' && nanobot gateway"
healthcheckPath = ""
restartPolicyType = "always"
